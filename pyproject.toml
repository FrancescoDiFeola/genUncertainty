[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "latent-uq-model-zoo"
version = "0.2.0"
description = "Task-agnostic latent LDM/LFM model zoo with base, aleatoric, and self-conditioning modes."
requires-python = ">=3.9"
