# This list mirrors [project.dependencies] and [project.optional-dependencies]
# in pyproject.toml, which remains the single source of truth for version
# constraints. Keep the two in sync if either one changes.

# Runtime dependencies
torch>=2.4.1
monai==1.5.2
monai-generative==0.2.3
numpy>=1.24,<3
einops>=0.7
PyYAML>=6
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
matplotlib>=3.7  # PNG training monitor (latent_uq/monitor.py)

# Testing (pyproject.toml's "dev" extra)
pytest>=8

# Optional: TensorBoard training previews/logging (pyproject.toml's "logging" extra)
tensorboard>=2.15

# Optional: building the BraTS volume cache (pyproject.toml's "brats" extra)
# nibabel>=5

# Installs this project itself (the latent_uq package and the latent-uq CLI).
-e .
